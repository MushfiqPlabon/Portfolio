// Animation constants
export const ANIMATION_DEFAULTS = {
  duration: 0.6,
  staggerDelay: 0.1,
  slideAmount: 50,
};

// Loading spinner sizes
export const LOADING_SPINNER_SIZES = {
  sm: "w-6 h-6",
  md: "w-10 h-10", 
  lg: "w-16 h-16"
};

// Default loading text
export const DEFAULT_LOADING_TEXT = "Loading...";

// Default color classes
export const DEFAULT_COLOR_CLASSES = {
  loadingSpinner: "text-primary-accent",
};